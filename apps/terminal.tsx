import Terminal from 'components/Terminal';

export const WindowComponent = () => {
  return <Terminal />;
};

export const appType = 'windowed';

export const startHeight = 600;

export const startWidth = 900;
